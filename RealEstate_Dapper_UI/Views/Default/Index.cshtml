
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}

@model List<ResultCategoryDto>
<section class="w3l-cover-3">
    <div class="cover top-bottom">
        <div class="container">
            <div class="middle-section text-center">
                <div class="section-width">
                    <p>Hayalinizdeki eve sahip olun!</p>
                    <h2>Bugün Sizin için Doğru Evi Bulalım</h2>
                    <div class="most-searches">
                        <h4>Sık Arananlar</h4>
                        <ul>
                            <li><a href="#link">Villa</a></li>
                            <li><a href="#link">Daire</a></li>
                            <li><a href="#link">Yazlık</a></li>
                        </ul>
                    </div>
                    <form action="/Property/PropertyListWithSearch" class="w3l-cover-3-gd" method="get">
                        <input type="text" name="searchKeyValue" placeholder="Anahtar kelimeyi girin" required>
                        <span class="input-group-btn">
                            <select class="btn btn-default" name="categoryId" required>
                                <option selected="">Gayrimenkul Türü</option>
                                @foreach (var item in Model)
                                {
                                    <option value="@item.CategoryID">@item.CategoryName</option>
                                }
                            </select>
                        </span>
                        <span class="input-group-btn">
                            <select class="btn btn-default" name="city" required>
                                <option selected="">Şehir Seçiniz</option>
                                <option>İstanbul</option>
                                <option>Paris</option>
                                <option>Milano</option>
                                <option>Roma</option>
                                <option>Lyon</option>
                                <option>Munih</option>
                                <option>Berlin</option>
                                <option>Sofya</option>
                                <option>İzmir</option>
                            </select>
                        </span>
                        <button type="submit" class="btn-primary">Ara</button>
                    </form>
                </div>
                <section id="bottom" class="demo">
                    <a href="#bottom"><span></span>İlan Listesi</a>
                </section>
            </div>
        </div>
    </div>
</section>

@* @await Component.InvokeAsync("_DefaultFeature") *@
@* @await Html.PartialAsync("~/Views/Default/PartialSearch.cshtml") *@
@await Component.InvokeAsync("_DefaultHomePageProductList")
@await Component.InvokeAsync("_DefaultWhoWeAre")
@await Component.InvokeAsync("_DefaultBottomGrid")
@await Component.InvokeAsync("_DefaultProductListExploreCities")
@await Component.InvokeAsync("_DefaultOurTestimonial")
@await Component.InvokeAsync("_DefaultSubFeatures")
@await Component.InvokeAsync("_DefaultDiscountOfDay")
@await Component.InvokeAsync("_DefaultBrand")


